{Indique Verdadero o Falso. Justifique en todos los casos:

a. Un módulo función no puede retornar un tipo de dato puntero a un arreglo.
Falso, un módulo función puede retornar un tipo de dato puntero a un arreglo ya que es un tipo de dato simple.

b. Si p es una variable de tipo puntero, entonces no es válida la siguiente instrucción: readln(p^);.
Falso, Se puede hacer un readln de un puntero siempre y cuando sea un tipo de datos simple.

c. Si un programa utiliza variables globales entonces no puede contener módulos que utilicen parámetros.
Falso, un programa puede tener variables globales y módulos que utilicen parámetros.

d. Un módulo con 5 líneas de código es más eficiente en tiempo de ejecución que un programa con 100 líneas de código.
Falso, la cantidad de líneas de código no determina la eficiencia en tiempo de ejecución.

e. Un tipo de dato registro puede ser homogéneo si todos sus campos son del mismo tipo de dato.
Verdadero, un tipo de dato registro puede ser homogéneo si todos sus campos son del mismo tipo de dato. Pero por lo general se utiliza para almacenar datos heterogéneos.

f. Nunca es posible acceder al nodo de la posición N en una estructura del tipo lista.
Falso, se puede acceder al nodo de la posición N en una estructura del tipo lista. Se puede recorrer la lista hasta llegar a la posición N o se puede utilizar un puntero auxiliar para recorrer la lista.
}
